package codes.chirag.emailclient.server.plugins

import io.ktor.server.application.*

fun Application.configureDatabases() {
    // Placeholder for database configuration (e.g. Exposed or SQLDelight)
    // val db = Database.connect("jdbc:sqlite:data.db", driver = "org.sqlite.JDBC")
}
